# example values

indexing-settings:
  sites:
    - url: http://www.sto-m52.ru
      name: СТО
    - url: https://playback.ru/
      name: PlayBack
    - url: https://s-102.edusite.ru/
      name: Школа
server:
  port: 8080

spring:
  datasource:
    username: root
    password: testtest
    url: jdbc:mysql://localhost:3306/search_engine?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true