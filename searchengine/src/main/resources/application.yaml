# example values

indexing-settings:
  sites:
    - url: https://kuzovsibir.ru/
      name: Кузовсиб
    - url: https://ds18nsk.edusite.ru/
      name: Школа
    - url: https://playback.ru/
      name: PlayBack
server:
  port: 8080

spring:
  datasource:
    username: root
    password: testtest
    url: jdbc:mysql://localhost:3306/search_engine?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    show-sql: true